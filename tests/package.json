{
  "name": "education-hub-e2e",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "npx playwright test",
    "test:install": "npx playwright install"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
